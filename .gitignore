# Секреты — не коммитить
.env
.env.*
!.env.example

# Hugging Face и прочие токены
**/secrets/
*.token

# Локальный key.properties (в репо — example + стабильный .jks)
grey_vless/android/key.properties

# Бинарники ядер (качает scripts/download-cores.sh в CI)
grey_vless/assets/bin/*
!grey_vless/assets/bin/.gitkeep
